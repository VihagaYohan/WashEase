import React from 'react';

// components
import {AppContainer, AppText} from '../../components';

const ProfileScreen = () => {
  return (
    <AppContainer>
      <AppText text="Profile screen" />
    </AppContainer>
  );
};

export default ProfileScreen;
