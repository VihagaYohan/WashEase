import React from 'react';

// components
import {AppContainer, AppText} from '../../components';

const BookingsScreen = () => {
  return (
    <AppContainer>
      <AppText text="Bookings screen" />
    </AppContainer>
  );
};

export default BookingsScreen;
