import React from 'react';
import {StyleSheet} from 'react-native';

// components
import {AppContainer, AppText} from '../../components';

const LoginScreen = () => {
  return (
    <AppContainer>
      <AppText text="Login screen" />
    </AppContainer>
  );
};

const styles = StyleSheet.create({});

export default LoginScreen;
